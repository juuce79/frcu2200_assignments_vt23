/**
 * Provides the {@code GameRunner} and {@code Replayer} classes which are responsible for issuing commands
 * to the {@code CommandManager}. These classes handle user interactions and replaying of game moves respectively.
 * {@code GameRunner} provides a user interface that handles user input, displays menu options, and issues commands
 * based on user actions. {@code Replayer} reads a log file and replays the game moves that are recorded in the log.
 *
 * @author Erik Ström, Frank Curwen
 */
package com.dt180g.laboration_3.issuers;
