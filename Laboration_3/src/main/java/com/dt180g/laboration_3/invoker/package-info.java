/**
 * Provides the {@code CommandManager} class which manages and executes commands in a game of Hanoi.
 * The {@code CommandManager} keeps track of both executed and unexecuted moves, allowing for undo and redo operations.
 * It interacts with the commands defined in the commands package to manipulate the game state.
 * 
 * @author Frank Curwen
 */
package com.dt180g.laboration_3.invoker;
